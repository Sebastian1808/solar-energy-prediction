/// Location model
class LocationModel {
  final double latitude;
  final double longitude;

  /// Instantiate a new Location with [latitude] and [longitude]
  LocationModel({
    required this.latitude,
    required this.longitude,
  });
}